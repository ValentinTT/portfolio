---
import SectionHeader from "../../SectionHeader.astro";
import SocialButtons from "./SocialButtons.astro";
const { default: MeBust } = await import("/src/assets/me-bust.svg?raw");
---

<section
  class="w-full max-w-full bg-transparent text-secondary-50 space-y-5 overflow-hidden pt-16"
  id="contact"
  data-color="#d1eafa"
  data-color-dark="#051e2e"
>
  <SectionHeader class="mb-14">CONTACT ME.</SectionHeader>
  <div class="flex justify-evenly flex-col md:flex-row">
    <div class="w-full max-w-2xl flex flex-col justify-center items-center">
      <h2 class="mb-4 text-2xl font-normal text-text dark:text-white">
        I would love to hear from you!
      </h2>
      <div class="max-w-[18rem] w-full">
        <Fragment set:html={MeBust} />
      </div>
      <div class="mt-5">
        <SocialButtons />
      </div>
    </div>
    <!-- <div class="h-fit w-full max-w-2xl p-5 md:p-12" id="form">
      <h2 class="mb-4 text-2xl font-normal text-text dark:text-white">
        Get in touch?
      </h2>
      <form id="contactForm">
        <div class="mb-6">
          <div class="mx-0 mb-1 sm:mb-4">
            <div class="mx-0 mb-1 sm:mb-4">
              <input
                type="text"
                id="name"
                autocomplete="given-name"
                placeholder="Your name or a.k.a"
                class="mb-2 w-full bg-transparent focus:outline-none focus:ring-0 border-b border-text-500 focus:border-text-600 py-2 pl-2 pr-4 text-text font-medium tracking-wide placeholder:text-gray-700 dark:placeholder:text-gray-200 dark:text-gray-300 sm:mb-0"
                name="name"
              />
            </div>
            <div class="mx-0 mb-1 sm:mb-4">
              <input
                type="email"
                id="email"
                autocomplete="email"
                placeholder="your.awesome@address.com"
                class="mb-2 w-full bg-transparent focus:outline-none focus:ring-0 border-b border-text-500 focus:border-text-600 py-2 pl-2 pr-4 text-text font-medium tracking-wide placeholder:text-gray-700 dark:placeholder:text-gray-200 dark:text-gray-300 sm:mb-0"
                name="email"
              />
            </div>
          </div>
          <div class="mx-0 mb-1 sm:mb-4">
            <textarea
              id="textarea"
              name="textarea"
              cols="30"
              rows="5"
              placeholder="Hey TT..."
              class="mb-2 w-full bg-transparent focus:outline-none focus:ring-0 border-b border-text-500 focus:border-text-600 py-2 pl-2 pr-4 text-text font-medium tracking-wide placeholder:text-gray-700 dark:placeholder:text-gray-200 dark:text-gray-300 sm:mb-0"
            ></textarea>
          </div>
        </div>
        <div>
          <button
            type="submit"
            class:list={[
              "bg-transparent border border-text-500 dark:border-emerald-500 w-full font-xl sm:mb-0 text-lg hover:shadow-md transition-shadow rounded-md p-2 dark:bg-text-300 text-text hover:text-white font-normal dark:text-text select-none",
              "relative before:content-[''] before:bg-text-500 before:absolute before:w-0 before:top-0 before:left-0 before:h-full before:transition-[width] before:duration-300 before:ease-linear before:-z-10 hover:before:w-full",
            ]}>Send Message</button
          >
        </div>
      </form>
    </div> -->
  </div>
</section>
